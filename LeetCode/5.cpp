#include <bits/stdc++.h>
using namespace std;

string longestPalindrome(string s) {
    vector<pair<int,int> > st_en;
    string revs(s);
    reverse(revs.begin(), revs.end());
    for (int i = 0; i < s.length(); i++) {
    	
	}
    
}

int main() {
	vector<pair<int,int> > myvec;
	
	return 0;
}
